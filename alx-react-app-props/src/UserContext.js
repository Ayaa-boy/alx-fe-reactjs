// src/UserContext.js
import React from 'react';

// Initialize and export the UserContext
const UserContext = React.createContext(null); // You can provide a default value if needed, but null is fine here

export default UserContext;